# 审计底稿 Roll Forward 工具 - 项目概要

## 1. 项目背景
审计交付中心经理需要一个自动化工具，将标准底稿模板通过"Roll Forward"方式生成新报告期的底稿，核心需求：
- 填入公司信息（名称、资产负债表日、TE、SAD等）
- 将上年底稿的"期末审定数"作为本年的"期初数"
- 复制K.01表的固定资产分类表头
- 批量处理多公司×多科目

## 2. 技术方案
- **技术栈**：Python + openpyxl
- **脚本文件**：`roll_forward.py`
- **工作目录**：`F:\AI专用\AI工作区\底稿roll forward\`

## 3. 输入文件结构

```
工作目录/
├── 标准底稿模板/           # 如：K1 SWP 固定资产 202YMMDD XYZ公司.xlsx
├── 上年底稿/               # 如：输入：K1 固定资产 20251231六六六有限公司.xlsx
├── PMTE_CRA信息表/         # 如：输入；参考资料.xlsx（含PMTE和CRA两个Sheet）
└── 输出目录/               # 生成的成品底稿
```

## 4. 已实现功能

### 4.1 Lead Sheet 处理（K.00）
- **表头填写**：客户名称、资产负债表日、分析日期、TE、SAD
- **期初数Roll Forward**：上年底稿I列（期末审定数）→ 本期J列（期初数）
- **表头定位**：自动搜索"期末审定数"字样，未找到时默认第48行
- **数据读取**：使用`data_only=True`模式读取计算后的值，避免复制公式

### 4.2 K.01 Agree SL to GL 处理
- **表头复制**：第10行固定资产分类（房屋建筑物、机器设备等）
- **年初余额**：只复制第12行"年初余额"的数据
  - 房屋建筑物账面数（E列）= 上年底稿G列审定数
  - 机器设备账面数（H列）= 上年底稿J列审定数
  - 运输设备账面数（K列）= 上年底稿M列审定数
  - 电子产品及通讯设备账面数（N列）= 上年底稿P列审定数
  - 合计账面数（Q列）= 上年底稿S列审定数
- **公式保留**：审定数列保留`=SUM()`公式，账面数列不保留公式
- **不复制**：购置、处置、在建工程转入等中间变动行

### 4.3 PMTE/CRA 信息提取
- **PMTE表**：公司名、层级、RP、PM、TE、SAD
- **CRA表**：按科目关键词搜索风险等级（目前实现基础版本，待完善精确匹配）

## 5. 脚本使用方式

```bash
python roll_forward.py \
  --template "K1 SWP 固定资产 202YMMDD XYZ公司.xlsx" \
  --prior "输入：K1 固定资产 20251231六六六有限公司.xlsx" \
  --pmte "输入；参考资料.xlsx" \
  --company "六六六有限公司" \
  --date "2026-12-31" \
  --output-dir "./output"
```

## 6. 待完成任务

### 6.1 待测试科目（10个）
1. 货币资金（A1）
2. 应付账款（B1）
3. 应付票据（E1）
4. 固定资产（K1）✅ 已测试
5. 无形资产
6. 长期待摊费用
7. 管理销售费用
8. 财务费用
9. 在建工程
10. 借款

### 6.2 待完善功能
- **CRA信息精确匹配**：目前按科目关键词搜索，需要按科目代码精确匹配
- **批量处理**：支持一次输入多个公司、多个科目，批量生成底稿
- **参数化科目映射**：不同科目的Sheet结构可能不同，需要配置映射规则
- **异常处理**：上年底稿缺少某些Sheet时的处理逻辑

## 7. 核心代码逻辑要点

### 7.1 文件打开策略
```python
# 标准模板（新底稿）
wb_new = openpyxl.load_workbook(template_path)

# 上年底稿：打开两次
wb_prior = openpyxl.load_workbook(prior_wb_path, data_only=False)  # 读取公式、表头
wb_prior_values = openpyxl.load_workbook(prior_wb_path, data_only=True)  # 读取计算值
```

### 7.2 Lead Sheet 期初数定位
- 表头行：搜索"期末审定数"字样（通常在第48行左右）
- 期末审定数列：I列（第9列）
- 期初数列：J列（第10列）

### 7.3 K.01 年初余额定位
- 表头行：第10行（固定资产类别）
- "年初余额"行：第12行（搜索C列"年初余额"字样）
- 各科目审定数列：G=7, J=10, M=13, P=16, S=19
- 各科目账面数列：E=5, H=8, K=11, N=14, Q=17

## 8. 测试验证文件
- **测试过的模板**：`K1 SWP 固定资产 202YMMDD XYZ公司.xlsx`
- **测试过的上年底稿**：`输入：K1 固定资产 20251231六六六有限公司.xlsx`
- **测试过的PMTE/CRA**：`输入；参考资料.xlsx`
- **验证脚本**：`verify_v2.py`, `verify_k01_formulas.py`

## 9. 注意事项
- 输出文件名会自动替换模板中的日期占位符（如`202YMMDD`→`20261231`）和公司名占位符
- 如果上年底稿被Excel打开，脚本会报错（文件被占用），需先关闭Excel
- CRA信息提取目前为简化版本，复杂场景可能需要手动调整

## 10. 下一步建议
1. 准备其他9个科目的标准底稿模板和上年底稿
2. 为每个科目配置K.01的列映射规则（因为不同科目的K.01结构可能不同）
3. 实现批量处理功能（读取目录中的全部公司×全部科目）
