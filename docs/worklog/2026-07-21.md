# 2026-07-21 工作总结

## 今日目标

- 完善 Audit Roll Forward 内测版的意见反馈入口。
- 修复生成底稿打开时 Excel 提示修复绘图 XML 的问题。
- 调整项目列表和公司列表页高度，使意见反馈角标无需滚动即可见。
- 以 Afix1 的完整依赖环境重新打包可对外分发的 Afix2。

## 已完成事项

- 在工具底部增加“意见反馈”问卷入口。
- 项目页与公司列表页改为仅按当前可见页面计算高度，不再被 CRA 等长页面撑高；项目或公司较多时由列表内部滚动。
- 在 openpyxl 保存后恢复标准模板的 drawings、media、工作表绘图关系和内容类型，保留图片与超链接，避免生成文件打开时修复 `drawing*.xml`。
- 保留 Q1 原有的 Note/限制性契约图片追加逻辑。
- 使用 Afix1 同一完整 Python 运行环境重新构建 Afix2，恢复 Pillow、lxml、NumPy 等依赖。
- 生成可分发文件 `dist/AuditRollForward_latestui_corefix5_crafix1_largefile1_feedback1_Afix2.exe`，大小 62,555,020 字节。
- 在对话中完成公司内部简要使用说明，补充功能、数据/Wording/CRA 迁移范围、支持科目、适用范围及反馈问卷提示。

## 主要修改文件

- `candidate_latestui_stablecore_20260715_1815/main_gui.py`
- `candidate_latestui_stablecore_20260715_1815/roll_forward_core.py`
- `build/specs/AuditRollForward_latestui_corefix5_crafix1_largefile1_feedback1.spec`
- `build/specs/AuditRollForward_latestui_corefix5_crafix1_largefile1_feedback1_Afix2.spec`
- `dist/AuditRollForward_latestui_corefix5_crafix1_largefile1_feedback1_Afix2.exe`（本地发布成品，不纳入 Git 提交）

## 验证或测试结果

- Python 语法编译检查通过。
- 1100×760 最小窗口下，项目列表和公司列表页根滚动条最大值为 0，意见反馈角标可见。
- 10 套内置模板的绘图、媒体与工作表引用一致性校验全部通过。
- Q1 和 U_exp VC&VD 按实际处理流程生成底稿后，使用本机 Excel 正常模式打开，均未生成修复日志。
- Afix2 与 Afix1 打包清单完全一致：675 个 Python 模块、84 个二进制库、127 个数据文件，无缺失项。
- 重新打包的 Afix2 EXE 已完成独立启动测试。

## 未完成事项、已知问题或风险

- Afix2 已在本机验证，尚未在第二台无开发环境的电脑上完成全流程烟雾测试。
- 工作区原有大量未提交缓存、历史测试输出、构建产物及旧改动，本次不清理、不恢复、不纳入提交。
- 62MB 的 Afix2 EXE 作为本地发布成品保留，为避免大二进制文件进入 Git 历史，不在本次提交中追踪。

## 明日建议与可直接继续的第一步

- 先将 Afix2 和必要说明文件复制到一台未安装 Python/开发依赖的公司电脑，执行一次 Q1 及一次其他科目的完整 Roll Forward，并确认 Excel 打开无修复提示。
- 通过后再按内部发布流程向公司用户分发。
